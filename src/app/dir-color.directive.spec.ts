import { DirColorDirective } from './dir-color.directive';

describe('DirColorDirective', () => {
  it('should create an instance', () => {
    const directive = new DirColorDirective();
    expect(directive).toBeTruthy();
  });
});
